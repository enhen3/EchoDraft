# 📤 上传到 GitHub 说明

## ✅ 已完成的准备工作

我已经帮你完成了以下工作：

1. ✅ 创建了 `.gitignore` 文件（自动忽略不需要的文件）
2. ✅ 更新了项目名称：`EchoTranscriber` → `EchoDraft`
   - ✅ README.md 已更新
   - ✅ app.py 窗口标题已更新
   - ✅ build_mac_app.sh 应用名称已更新
3. ✅ 创建了自动上传脚本 `upload_to_github.sh`

## 🚀 如何上传到 GitHub

### 方法一：运行自动上传脚本（推荐，最简单）

打开终端，运行：

```bash
cd /Users/haoyangs/EchoDraft
chmod +x upload_to_github.sh
./upload_to_github.sh
```

脚本会自动完成：
1. 清理不需要的文件
2. 初始化 Git 仓库
3. 配置远程仓库（https://github.com/enhen3/EchoDraft.git）
4. 添加并提交所有文件
5. 推送到 GitHub

**就这么简单！** 🎉

### 方法二：手动操作

如果你想手动控制每一步：

```bash
cd /Users/haoyangs/EchoDraft

# 1. 清理文件
rm -rf build dist test_unzip *.zip test_silence.wav
find . -name ".DS_Store" -delete

# 2. 初始化 Git
git init

# 3. 添加远程仓库
git remote add origin https://github.com/enhen3/EchoDraft.git

# 4. 添加并提交文件
git add .
git commit -m "Initial commit: EchoDraft - 本地语音转写工具"

# 5. 推送到 GitHub
git branch -M main
git push -u origin main
```

## 📦 将要上传的文件

上传后，你的GitHub仓库将包含：

**源代码：**
- `app.py` - 主程序（GUI + CLI）
- `whisper_local.py` - Whisper 推理引擎
- `config.py` - 配置管理
- `audio_utils.py` - 音频工具
- `cli.py` - CLI 入口

**配置文件：**
- `requirements.txt` - Python 依赖
- `build_mac_app.sh` - macOS 打包脚本
- `.gitignore` - Git 忽略规则

**文档：**
- `README.md` - 项目说明
- `AGENTS.md` - 开发规范
- `故障排除指南.txt` - 问题解决指南
- `分享清单-v2.txt` - 分享说明

**目录结构：**
- `models/` - 模型目录（空，用户需自行下载）
- `output/` - 输出目录（空）

## ⚠️ 不会上传的文件

`.gitignore` 已配置自动忽略这些文件：

- ❌ `build/` 和 `dist/` 目录（构建产物）
- ❌ `*.zip` 文件（压缩包太大）
- ❌ `models/whisper/small-int8/*.bin` （模型文件太大，461 MB）
- ❌ `.DS_Store` 文件（macOS 系统文件）
- ❌ `__pycache__/` （Python 缓存）
- ❌ 测试文件

这样仓库会保持干净，只包含源代码和必要的文档。

## 🎯 上传后的下一步

### 1. 在GitHub页面添加说明

访问：https://github.com/enhen3/EchoDraft

- 添加仓库描述：`🎙️ 本地语音转写工具 - 使用 Whisper small 模型将音频转录为文字，支持多语言识别，完全离线运行`
- 添加 Topics 标签：`whisper`, `speech-to-text`, `transcription`, `audio-processing`, `pyqt6`, `macos`, `python`, `ai`

### 2. 关于大型文件（模型和压缩包）

**模型文件（461 MB）**
- 太大，不适合放在 Git 仓库中
- README.md 已包含下载说明
- 用户可以自己从 Hugging Face 下载

**压缩包（EchoDraft-macOS-v2.zip，596 MB）**
- 如果想分享打包好的应用，建议使用 GitHub Release
- 创建一个 Release，上传 zip 文件
- 这样不会占用仓库空间

### 3. 创建 Release（可选）

如果你想分享打包好的应用：

```bash
# 使用 GitHub CLI
gh release create v1.0.0 EchoDraft-macOS-v2.zip \
  --title "EchoDraft v1.0.0 - macOS 应用" \
  --notes "完整的 macOS 应用，包含 Whisper 模型，下载解压即用"
```

或在 GitHub 网站上手动创建 Release。

## 🆘 遇到问题？

### 问题1：Permission denied

```bash
chmod +x upload_to_github.sh
```

### 问题2：Already exists

如果提示远程仓库已存在：
```bash
git remote set-url origin https://github.com/enhen3/EchoDraft.git
```

### 问题3：需要登录

确保你已经配置了 Git 凭证：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

如果是私有仓库，可能需要创建 Personal Access Token。

## 🎉 完成！

运行上传脚本后，访问：
**https://github.com/enhen3/EchoDraft**

你应该能看到你的项目了！

---

祝你使用愉快！🚀
